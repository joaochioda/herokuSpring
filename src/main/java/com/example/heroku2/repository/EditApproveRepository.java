package com.example.heroku2.repository;

import com.example.heroku2.model.EditApprove;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EditApproveRepository extends JpaRepository<EditApprove, Long> {
}
